// Legacy root build file — used by the default ./gradlew (Gradle 7.3.1) for the 1.15.2-1.18
// modules. The 1.21.1 modules use build-1.21.1.gradle via ./gradlew-1.21.1.

apply plugin: 'idea'

version = project.mod_version
group = project.maven_group

def tagPrefix = "refs/tags/"
if ((System.getenv("CI_VERSION") ?: "").startsWith(tagPrefix)) {
    version = System.getenv("CI_VERSION").drop(tagPrefix.length()).takeWhile { it != '+' }
}
